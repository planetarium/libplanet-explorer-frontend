overwrite: true
generates:
  src/gql/:
    schema: ./src/schema.graphql
    documents: ./src/api.graphql
    preset: client
    config:
      scalars:
        BigInt: BigInt
  src/explorerGql/:  # FIXME: swap with 9c
    schema: ./src/explorerSchema.graphql
    documents: ./src/explorerApi.graphql
    preset: client
    config:
      scalars:
        BigInt: BigInt
