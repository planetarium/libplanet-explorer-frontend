import styled from '@emotion/styled';

export const wrapperWidth = 960;

const Wrapper = styled.div`
  margin: 0 auto;
  padding: 2rem 0;
  width: 100%;
  max-width: ${wrapperWidth}px;
`;

export default Wrapper;
